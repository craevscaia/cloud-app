# Cloud Lab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Important user override:** Do NOT run `git commit`, `git push`, or amend commits at any point. The "commit" steps in standard plans are replaced here with "checkpoint" steps — verify, then move on. The user controls git history.

**Goal:** Take the existing Spring Boot "Hello World" app and ship the full cloud lab: containerized, published to GHCR, deployed to Kubernetes both locally (`kind`) and on DigitalOcean Kubernetes Service (DOKS), with rolling updates, rollback, HPA, centralized logging via Loki, metrics via Prometheus + Grafana, and a separate Postgres container with persistent storage.

**Architecture:** Same Kustomize base manifests for both environments; overlay-only differences. `k8s/local/` for kind, `k8s/remote/` for DOKS. App extended with `/messages` GET+POST backed by Postgres (JdbcTemplate + Flyway), Actuator/Micrometer metrics, JSON logs scraped by promtail → Loki. Helm-installed ingress-nginx, kube-prometheus-stack, loki-stack. CI builds + pushes to `ghcr.io/infigo-adrian/cloud-app`. Manual `kubectl apply -k` per env.

**Tech Stack:** Spring Boot 3.2 (Kotlin), JdbcTemplate, Flyway, Postgres 16, Micrometer, logstash-logback-encoder, Docker multi-stage, kind, DOKS, Kustomize, Helm, ingress-nginx, kube-prometheus-stack, loki-stack, GitHub Actions.

**Spec:** `docs/superpowers/specs/2026-05-26-cloud-lab-design.md`

---

## File Structure

| Path | Responsibility |
|---|---|
| `build.gradle.kts` | Add Actuator, JDBC, Micrometer-Prometheus, Flyway, Postgres, logstash encoder, H2 (test) |
| `src/main/kotlin/md/utm/cloudapp/messages/Message.kt` | `data class Message` |
| `src/main/kotlin/md/utm/cloudapp/messages/MessageRepository.kt` | `JdbcTemplate` repo: `findAll`, `insert` |
| `src/main/kotlin/md/utm/cloudapp/messages/MessagesController.kt` | `GET /messages`, `POST /messages` |
| `src/main/resources/application.properties` | Datasource env vars, Actuator probes + Prometheus |
| `src/main/resources/application-test.properties` | H2 datasource for tests |
| `src/main/resources/db/migration/V1__messages.sql` | Create `messages` table |
| `src/main/resources/logback-spring.xml` | Console (local) / JSON (default) appender split |
| `src/test/kotlin/md/utm/cloudapp/messages/MessagesControllerIT.kt` | `@SpringBootTest` round-trip test |
| `docker/Dockerfile` | Multi-stage build, non-root, JRE 17 |
| `.dockerignore` | Exclude `.gradle/`, `build/`, IDE files |
| `k8s/base/deployment.yaml` | App Deployment, probes, resources, rolling strategy |
| `k8s/base/service.yaml` | ClusterIP for app |
| `k8s/base/ingress.yaml` | `ingressClassName: nginx`, host placeholder |
| `k8s/base/hpa.yaml` | min 2 / max 6, CPU 70% + memory 80% |
| `k8s/base/configmap.yaml` | `SPRING_DATASOURCE_URL` |
| `k8s/base/secret.yaml` | Stub; overlays generate the real one |
| `k8s/base/servicemonitor.yaml` | Prometheus scrape target |
| `k8s/base/postgres-statefulset.yaml` | Postgres pod + PVC |
| `k8s/base/postgres-service.yaml` | Headless Service for Postgres |
| `k8s/base/kustomization.yaml` | Resource list + common labels |
| `k8s/local/kustomization.yaml` | kind overlay (resources, image tag, ingress host, storage class, Secret) |
| `k8s/local/deployment-patch.yaml` | Lower resources |
| `k8s/local/ingress-patch.yaml` | host `localhost` |
| `k8s/remote/kustomization.yaml` | DOKS overlay |
| `k8s/remote/deployment-patch.yaml` | Image tag `:latest` |
| `k8s/remote/ingress-patch.yaml` | host substituted by bootstrap script |
| `k8s/remote/postgres-patch.yaml` | Storage class `do-block-storage` |
| `charts/ingress-nginx/values.yaml` | NGINX ingress Helm values |
| `charts/kube-prometheus-stack/values.yaml` | Prometheus/Grafana Helm values |
| `charts/loki-stack/values.yaml` | Loki + promtail values |
| `k8s/base/grafana-dashboards/app-dashboard.yaml` | JVM/HTTP dashboard ConfigMap |
| `k8s/base/grafana-dashboards/logs-dashboard.yaml` | Loki dashboard ConfigMap |
| `scripts/kind-config.yaml` | kind cluster config (port mappings 80/443) |
| `scripts/bootstrap-local.sh` | One-shot local bring-up + smoke test |
| `scripts/bootstrap-remote.sh` | One-shot DOKS bring-up + smoke test |
| `scripts/teardown-remote.sh` | `doctl kubernetes cluster delete cloud-lab` |
| `.github/workflows/build-and-push.yml` | CI: test + build + push to GHCR |
| `docs/demo-runbook.md` | One command per lab requirement for the grader |
| `.gitignore` | Add `.env.remote` |

---

## Task 1: Add Spring Boot dependencies and Actuator config

**Files:**
- Modify: `build.gradle.kts`
- Modify: `src/main/resources/application.properties`
- Create: `src/main/resources/application-test.properties`

- [ ] **Step 1: Add dependencies to `build.gradle.kts`**

Replace the `dependencies { ... }` block with:

```kotlin
dependencies {
    implementation("org.springframework.boot:spring-boot-starter-web")
    implementation("org.springframework.boot:spring-boot-starter-actuator")
    implementation("org.springframework.boot:spring-boot-starter-jdbc")
    implementation("com.fasterxml.jackson.module:jackson-module-kotlin")
    implementation("org.jetbrains.kotlin:kotlin-reflect")
    implementation("io.micrometer:micrometer-registry-prometheus")
    implementation("org.flywaydb:flyway-core")
    implementation("org.flywaydb:flyway-database-postgresql")
    runtimeOnly("org.postgresql:postgresql")
    implementation("net.logstash.logback:logstash-logback-encoder:7.4")
    testImplementation("org.springframework.boot:spring-boot-starter-test")
    testRuntimeOnly("com.h2database:h2")
}
```

- [ ] **Step 2: Append Actuator + datasource config to `src/main/resources/application.properties`**

```properties
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:mem:cloudapp}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sa}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.probes.enabled=true
management.health.livenessstate.enabled=true
management.health.readinessstate.enabled=true
spring.flyway.enabled=true
```

- [ ] **Step 3: Create `src/main/resources/application-test.properties`**

```properties
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.flyway.enabled=false
```

- [ ] **Step 4: Verify it still compiles**

Run: `./gradlew compileKotlin`
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 5: Checkpoint** — leave files modified, no commit.

---

## Task 2: Add JSON logging via logback-spring.xml

**Files:**
- Create: `src/main/resources/logback-spring.xml`

- [ ] **Step 1: Create the logback config**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProfile name="local">
        <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
        </root>
    </springProfile>
    <springProfile name="!local">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeContext>true</includeContext>
                <includeMdc>true</includeMdc>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>
</configuration>
```

- [ ] **Step 2: Smoke-check**

Run: `./gradlew bootRun --args='--spring.profiles.active=local'` in another terminal, hit `http://localhost:8080/`, confirm pretty logs. Then stop and re-run without `--spring.profiles.active=local` — confirm JSON output. (Optional but recommended.)

- [ ] **Step 3: Checkpoint.**

---

## Task 3: Add Flyway migration + Message domain (TDD)

**Files:**
- Create: `src/main/resources/db/migration/V1__messages.sql`
- Create: `src/main/kotlin/md/utm/cloudapp/messages/Message.kt`
- Create: `src/main/kotlin/md/utm/cloudapp/messages/MessageRepository.kt`
- Create: `src/test/kotlin/md/utm/cloudapp/messages/MessagesControllerIT.kt`
- Create: `src/main/kotlin/md/utm/cloudapp/messages/MessagesController.kt`

- [ ] **Step 1: Write the failing integration test**

Create `src/test/kotlin/md/utm/cloudapp/messages/MessagesControllerIT.kt`:

```kotlin
package md.utm.cloudapp.messages

import org.junit.jupiter.api.Test
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.http.MediaType
import org.springframework.test.context.ActiveProfiles
import org.springframework.test.web.servlet.MockMvc
import org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get
import org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post
import org.springframework.test.web.servlet.result.MockMvcResultMatchers.content
import org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath
import org.springframework.test.web.servlet.result.MockMvcResultMatchers.status
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc
import org.springframework.jdbc.core.JdbcTemplate

@SpringBootTest
@AutoConfigureMockMvc
@ActiveProfiles("test")
class MessagesControllerIT @Autowired constructor(
    private val mockMvc: MockMvc,
    private val jdbc: JdbcTemplate,
) {
    @org.junit.jupiter.api.BeforeEach
    fun setup() {
        jdbc.execute(
            "CREATE TABLE IF NOT EXISTS messages (" +
                "id BIGINT AUTO_INCREMENT PRIMARY KEY, " +
                "text VARCHAR(1000) NOT NULL, " +
                "created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP)"
        )
        jdbc.execute("DELETE FROM messages")
    }

    @Test
    fun `POST then GET round-trips a message`() {
        mockMvc.perform(
            post("/messages")
                .contentType(MediaType.APPLICATION_JSON)
                .content("""{"text":"hello"}""")
        )
            .andExpect(status().isCreated)
            .andExpect(jsonPath("$.text").value("hello"))
            .andExpect(jsonPath("$.id").isNumber)

        mockMvc.perform(get("/messages"))
            .andExpect(status().isOk)
            .andExpect(jsonPath("$[0].text").value("hello"))
    }

    @Test
    fun `POST with empty text returns 400`() {
        mockMvc.perform(
            post("/messages")
                .contentType(MediaType.APPLICATION_JSON)
                .content("""{"text":""}""")
        ).andExpect(status().isBadRequest)
    }
}
```

- [ ] **Step 2: Run the test, confirm it fails**

Run: `./gradlew test --tests "*MessagesControllerIT*"`
Expected: FAIL — controller doesn't exist yet, `MessagesController` cannot be loaded.

- [ ] **Step 3: Create the Flyway migration**

Create `src/main/resources/db/migration/V1__messages.sql`:

```sql
CREATE TABLE messages (
    id BIGSERIAL PRIMARY KEY,
    text TEXT NOT NULL CHECK (length(text) > 0),
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

- [ ] **Step 4: Create the `Message` data class**

Create `src/main/kotlin/md/utm/cloudapp/messages/Message.kt`:

```kotlin
package md.utm.cloudapp.messages

import java.time.Instant

data class Message(
    val id: Long?,
    val text: String,
    val createdAt: Instant?,
)
```

- [ ] **Step 5: Create the repository**

Create `src/main/kotlin/md/utm/cloudapp/messages/MessageRepository.kt`:

```kotlin
package md.utm.cloudapp.messages

import org.springframework.jdbc.core.JdbcTemplate
import org.springframework.jdbc.support.GeneratedKeyHolder
import org.springframework.stereotype.Repository
import java.sql.PreparedStatement
import java.sql.Statement
import java.time.Instant

@Repository
class MessageRepository(private val jdbc: JdbcTemplate) {

    fun findAll(): List<Message> = jdbc.query(
        "SELECT id, text, created_at FROM messages ORDER BY id DESC"
    ) { rs, _ ->
        Message(
            id = rs.getLong("id"),
            text = rs.getString("text"),
            createdAt = rs.getTimestamp("created_at").toInstant(),
        )
    }

    fun insert(text: String): Message {
        val keyHolder = GeneratedKeyHolder()
        jdbc.update({ conn ->
            val ps: PreparedStatement = conn.prepareStatement(
                "INSERT INTO messages(text) VALUES (?)",
                Statement.RETURN_GENERATED_KEYS,
            )
            ps.setString(1, text)
            ps
        }, keyHolder)
        val id = keyHolder.keys?.get("id") as? Number
            ?: keyHolder.key
            ?: error("No generated key returned")
        return Message(id = id.toLong(), text = text, createdAt = Instant.now())
    }
}
```

- [ ] **Step 6: Create the controller**

Create `src/main/kotlin/md/utm/cloudapp/messages/MessagesController.kt`:

```kotlin
package md.utm.cloudapp.messages

import org.springframework.http.HttpStatus
import org.springframework.http.ResponseEntity
import org.springframework.web.bind.annotation.GetMapping
import org.springframework.web.bind.annotation.PostMapping
import org.springframework.web.bind.annotation.RequestBody
import org.springframework.web.bind.annotation.RequestMapping
import org.springframework.web.bind.annotation.RestController

data class CreateMessageRequest(val text: String)

@RestController
@RequestMapping("/messages")
class MessagesController(private val repo: MessageRepository) {

    @GetMapping
    fun list(): List<Message> = repo.findAll()

    @PostMapping
    fun create(@RequestBody body: CreateMessageRequest): ResponseEntity<Message> {
        if (body.text.isBlank()) {
            return ResponseEntity.badRequest().build()
        }
        return ResponseEntity.status(HttpStatus.CREATED).body(repo.insert(body.text))
    }
}
```

- [ ] **Step 7: Run the test, confirm it passes**

Run: `./gradlew test --tests "*MessagesControllerIT*"`
Expected: PASS — both test cases green.

- [ ] **Step 8: Run all tests**

Run: `./gradlew test`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 9: Checkpoint.**

---

## Task 4: Dockerfile and local image build

**Files:**
- Create: `docker/Dockerfile`
- Create: `.dockerignore`

- [ ] **Step 1: Create `.dockerignore`**

```
.gradle/
build/
.idea/
.vscode/
*.iml
.git/
docs/
k8s/
charts/
scripts/
```

- [ ] **Step 2: Create `docker/Dockerfile`**

```dockerfile
# syntax=docker/dockerfile:1.6
FROM eclipse-temurin:17-jdk-alpine AS build
WORKDIR /app
COPY gradlew settings.gradle.kts build.gradle.kts ./
COPY gradle ./gradle
RUN ./gradlew --no-daemon dependencies > /dev/null 2>&1 || true
COPY src ./src
RUN ./gradlew --no-daemon bootJar

FROM eclipse-temurin:17-jre-alpine
RUN addgroup -S app && adduser -S app -G app
WORKDIR /app
COPY --from=build /app/build/libs/*.jar /app/app.jar
USER app
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
```

- [ ] **Step 3: Build the image locally**

Run: `docker build -t cloud-app:local -f docker/Dockerfile .`
Expected: image built; final line `Successfully tagged cloud-app:local` (or buildkit equivalent).

- [ ] **Step 4: Smoke-run the image with H2 (no Postgres yet)**

Run:
```bash
docker run --rm -p 8080:8080 \
  -e SPRING_PROFILES_ACTIVE=local \
  cloud-app:local
```
In a second terminal: `curl http://localhost:8080/` → "Hello World!"
Stop the container with Ctrl+C.

- [ ] **Step 5: Checkpoint.**

---

## Task 5: Kubernetes base manifests — app

**Files:**
- Create: `k8s/base/deployment.yaml`
- Create: `k8s/base/service.yaml`
- Create: `k8s/base/ingress.yaml`
- Create: `k8s/base/hpa.yaml`
- Create: `k8s/base/configmap.yaml`
- Create: `k8s/base/secret.yaml`
- Create: `k8s/base/servicemonitor.yaml`

- [ ] **Step 1: `k8s/base/deployment.yaml`**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-app
  labels:
    app.kubernetes.io/name: cloud-app
spec:
  replicas: 2
  revisionHistoryLimit: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: cloud-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cloud-app
    spec:
      containers:
        - name: cloud-app
          image: ghcr.io/infigo-adrian/cloud-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: cloud-app-config
            - secretRef:
                name: cloud-app-secret
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
```

- [ ] **Step 2: `k8s/base/service.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: cloud-app
  labels:
    app.kubernetes.io/name: cloud-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cloud-app
  ports:
    - name: http
      port: 8080
      targetPort: http
```

- [ ] **Step 3: `k8s/base/ingress.yaml`**

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-app
spec:
  ingressClassName: nginx
  rules:
    - host: cloud-app.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cloud-app
                port:
                  number: 8080
```

- [ ] **Step 4: `k8s/base/hpa.yaml`**

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cloud-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cloud-app
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
```

- [ ] **Step 5: `k8s/base/configmap.yaml`**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloud-app-config
data:
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/cloudapp
```

- [ ] **Step 6: `k8s/base/secret.yaml`** (stub — overlays generate the real one)

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: cloud-app-secret
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: cloudapp
  SPRING_DATASOURCE_PASSWORD: changeme
  POSTGRES_USER: cloudapp
  POSTGRES_PASSWORD: changeme
  POSTGRES_DB: cloudapp
```

- [ ] **Step 7: `k8s/base/servicemonitor.yaml`**

```yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cloud-app
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: cloud-app
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
```

The `release: monitoring` label matches the default selector used by `kube-prometheus-stack` (Helm release name `monitoring`).

- [ ] **Step 8: Checkpoint.**

---

## Task 6: Kubernetes base manifests — Postgres

**Files:**
- Create: `k8s/base/postgres-statefulset.yaml`
- Create: `k8s/base/postgres-service.yaml`

- [ ] **Step 1: `k8s/base/postgres-statefulset.yaml`**

```yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - name: pg
              containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: cloud-app-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cloud-app-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: cloud-app-secret
                  key: POSTGRES_DB
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "cloudapp", "-d", "cloudapp"]
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
```

- [ ] **Step 2: `k8s/base/postgres-service.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: pg
      port: 5432
      targetPort: pg
```

- [ ] **Step 3: Checkpoint.**

---

## Task 7: Kustomize base wiring

**Files:**
- Create: `k8s/base/kustomization.yaml`

- [ ] **Step 1: Write the kustomization**

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/part-of: cloud-lab
resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - servicemonitor.yaml
  - postgres-statefulset.yaml
  - postgres-service.yaml
```

- [ ] **Step 2: Validate the base renders**

Run: `kubectl kustomize k8s/base | head -60`
Expected: well-formed YAML output, no errors. (Just rendering — not applying anywhere yet.)

- [ ] **Step 3: Checkpoint.**

---

## Task 8: Local overlay (`k8s/local`)

**Files:**
- Create: `k8s/local/kustomization.yaml`
- Create: `k8s/local/deployment-patch.yaml`
- Create: `k8s/local/ingress-patch.yaml`

- [ ] **Step 1: `k8s/local/deployment-patch.yaml`**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-app
spec:
  template:
    spec:
      containers:
        - name: cloud-app
          image: cloud-app:local
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi
```

- [ ] **Step 2: `k8s/local/ingress-patch.yaml`**

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-app
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cloud-app
                port:
                  number: 8080
```

- [ ] **Step 3: `k8s/local/kustomization.yaml`**

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ../base
patches:
  - path: deployment-patch.yaml
  - path: ingress-patch.yaml
secretGenerator:
  - name: cloud-app-secret
    behavior: replace
    literals:
      - SPRING_DATASOURCE_USERNAME=cloudapp
      - SPRING_DATASOURCE_PASSWORD=devpassword
      - POSTGRES_USER=cloudapp
      - POSTGRES_PASSWORD=devpassword
      - POSTGRES_DB=cloudapp
generatorOptions:
  disableNameSuffixHash: true
```

- [ ] **Step 4: Validate**

Run: `kubectl kustomize k8s/local | grep -E "image:|host:|cpu:" | head`
Expected: see `image: cloud-app:local`, `host: localhost`, `cpu: 100m`.

- [ ] **Step 5: Checkpoint.**

---

## Task 9: Remote overlay (`k8s/remote`)

**Files:**
- Create: `k8s/remote/kustomization.yaml`
- Create: `k8s/remote/deployment-patch.yaml`
- Create: `k8s/remote/ingress-patch.yaml`
- Create: `k8s/remote/postgres-patch.yaml`
- Modify: `.gitignore` (add `.env.remote`)

- [ ] **Step 1: `k8s/remote/deployment-patch.yaml`**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-app
spec:
  template:
    spec:
      containers:
        - name: cloud-app
          image: ghcr.io/infigo-adrian/cloud-app:latest
          imagePullPolicy: Always
```

- [ ] **Step 2: `k8s/remote/ingress-patch.yaml`** (host substituted by bootstrap script using `sed`)

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-app
spec:
  rules:
    - host: CLOUD_APP_HOST
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cloud-app
                port:
                  number: 8080
```

- [ ] **Step 3: `k8s/remote/postgres-patch.yaml`**

```yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
spec:
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: do-block-storage
        resources:
          requests:
            storage: 2Gi
```

- [ ] **Step 4: `k8s/remote/kustomization.yaml`**

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ../base
patches:
  - path: deployment-patch.yaml
  - path: ingress-patch.yaml
  - path: postgres-patch.yaml
    target:
      kind: StatefulSet
      name: postgres
secretGenerator:
  - name: cloud-app-secret
    behavior: replace
    envs:
      - .env.remote
generatorOptions:
  disableNameSuffixHash: true
```

- [ ] **Step 5: Add `.env.remote` to `.gitignore`**

Append to `.gitignore`:
```
.env.remote
```

- [ ] **Step 6: Create `.env.remote.example` (committed example, real `.env.remote` is gitignored)**

Create `.env.remote.example`:
```
SPRING_DATASOURCE_USERNAME=cloudapp
SPRING_DATASOURCE_PASSWORD=replace-me-with-strong-password
POSTGRES_USER=cloudapp
POSTGRES_PASSWORD=replace-me-with-strong-password
POSTGRES_DB=cloudapp
```

- [ ] **Step 7: Checkpoint.**

---

## Task 10: Helm chart values

**Files:**
- Create: `charts/ingress-nginx/values.yaml`
- Create: `charts/kube-prometheus-stack/values.yaml`
- Create: `charts/loki-stack/values.yaml`

- [ ] **Step 1: `charts/ingress-nginx/values.yaml`**

```yaml
controller:
  service:
    type: LoadBalancer
  ingressClassResource:
    default: true
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      additionalLabels:
        release: monitoring
```

- [ ] **Step 2: `charts/kube-prometheus-stack/values.yaml`**

```yaml
grafana:
  adminPassword: admin
  sidecar:
    dashboards:
      enabled: true
      label: grafana_dashboard
      labelValue: "1"
      searchNamespace: ALL
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - grafana.localhost
    path: /
prometheus:
  prometheusSpec:
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
defaultRules:
  create: true
alertmanager:
  enabled: false
```

- [ ] **Step 3: `charts/loki-stack/values.yaml`**

```yaml
loki:
  enabled: true
  persistence:
    enabled: false
promtail:
  enabled: true
grafana:
  enabled: false
prometheus:
  enabled: false
```

- [ ] **Step 4: Checkpoint.**

---

## Task 11: Grafana dashboards (ConfigMaps)

**Files:**
- Create: `k8s/base/grafana-dashboards/app-dashboard.yaml`
- Create: `k8s/base/grafana-dashboards/logs-dashboard.yaml`
- Modify: `k8s/base/kustomization.yaml` to include them

- [ ] **Step 1: `k8s/base/grafana-dashboards/app-dashboard.yaml`**

Minimal dashboard with JVM memory + HTTP request rate. Use the inlined JSON below.

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloud-app-dashboard
  labels:
    grafana_dashboard: "1"
data:
  app-dashboard.json: |
    {
      "title": "Cloud App",
      "schemaVersion": 38,
      "panels": [
        {
          "type": "timeseries",
          "title": "HTTP requests/sec",
          "targets": [
            {
              "expr": "sum(rate(http_server_requests_seconds_count{job=\"cloud-app\"}[1m])) by (method, status)",
              "legendFormat": "{{method}} {{status}}"
            }
          ],
          "gridPos": {"x":0,"y":0,"w":24,"h":8}
        },
        {
          "type": "timeseries",
          "title": "JVM heap used",
          "targets": [
            {
              "expr": "jvm_memory_used_bytes{area=\"heap\",job=\"cloud-app\"}",
              "legendFormat": "{{id}}"
            }
          ],
          "gridPos": {"x":0,"y":8,"w":24,"h":8}
        }
      ]
    }
```

- [ ] **Step 2: `k8s/base/grafana-dashboards/logs-dashboard.yaml`**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloud-app-logs-dashboard
  labels:
    grafana_dashboard: "1"
data:
  logs-dashboard.json: |
    {
      "title": "Cloud App Logs",
      "schemaVersion": 38,
      "panels": [
        {
          "type": "logs",
          "title": "App logs (Loki)",
          "datasource": {"type":"loki","uid":"loki"},
          "targets": [
            {
              "expr": "{app_kubernetes_io_name=\"cloud-app\"}"
            }
          ],
          "gridPos": {"x":0,"y":0,"w":24,"h":16}
        }
      ]
    }
```

- [ ] **Step 3: Update `k8s/base/kustomization.yaml`** — add the two files to `resources:`

```yaml
resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - servicemonitor.yaml
  - postgres-statefulset.yaml
  - postgres-service.yaml
  - grafana-dashboards/app-dashboard.yaml
  - grafana-dashboards/logs-dashboard.yaml
```

- [ ] **Step 4: Validate**

Run: `kubectl kustomize k8s/base | grep "kind: ConfigMap"`
Expected: three ConfigMaps (`cloud-app-config`, `cloud-app-dashboard`, `cloud-app-logs-dashboard`).

- [ ] **Step 5: Checkpoint.**

---

## Task 12: Local bootstrap script

**Files:**
- Create: `scripts/kind-config.yaml`
- Create: `scripts/bootstrap-local.sh`

- [ ] **Step 1: `scripts/kind-config.yaml`**

```yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"
    extraPortMappings:
      - containerPort: 80
        hostPort: 80
        protocol: TCP
      - containerPort: 443
        hostPort: 443
        protocol: TCP
```

- [ ] **Step 2: `scripts/bootstrap-local.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

CLUSTER_NAME="cloud-lab"
IMAGE_TAG="cloud-app:local"

echo ">>> 1/8 Ensuring kind cluster exists"
if ! kind get clusters | grep -qx "$CLUSTER_NAME"; then
  kind create cluster --name "$CLUSTER_NAME" --config scripts/kind-config.yaml
fi
kubectl config use-context "kind-$CLUSTER_NAME"

echo ">>> 2/8 Installing ingress-nginx"
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx >/dev/null 2>&1 || true
helm repo update >/dev/null
helm upgrade --install ingress-nginx ingress-nginx/ingress-nginx \
  -n ingress-nginx --create-namespace \
  -f charts/ingress-nginx/values.yaml \
  --set controller.hostPort.enabled=true \
  --set controller.service.type=NodePort \
  --wait

echo ">>> 3/8 Installing kube-prometheus-stack"
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts >/dev/null 2>&1 || true
helm repo update >/dev/null
helm upgrade --install monitoring prometheus-community/kube-prometheus-stack \
  -n monitoring --create-namespace \
  -f charts/kube-prometheus-stack/values.yaml \
  --wait

echo ">>> 4/8 Installing loki-stack"
helm repo add grafana https://grafana.github.io/helm-charts >/dev/null 2>&1 || true
helm repo update >/dev/null
helm upgrade --install loki grafana/loki-stack \
  -n monitoring \
  -f charts/loki-stack/values.yaml \
  --wait

echo ">>> 5/8 Building app image"
docker build -t "$IMAGE_TAG" -f docker/Dockerfile .

echo ">>> 6/8 Loading image into kind"
kind load docker-image "$IMAGE_TAG" --name "$CLUSTER_NAME"

echo ">>> 7/8 Applying k8s/local overlay"
kubectl apply -k k8s/local
kubectl rollout status deployment/cloud-app --timeout=180s

echo ">>> 8/8 Smoke test"
sleep 5
curl -sf http://localhost/ && echo
curl -sf -X POST http://localhost/messages \
  -H 'Content-Type: application/json' \
  -d '{"text":"hello from bootstrap"}' && echo
curl -sf http://localhost/messages && echo

echo "Done. Grafana: kubectl -n monitoring port-forward svc/monitoring-grafana 3000:80 (admin/admin)"
```

- [ ] **Step 3: Make it executable**

Run: `chmod +x scripts/bootstrap-local.sh`

- [ ] **Step 4: Checkpoint.** (We'll actually run this at the end in Task 16.)

---

## Task 13: Remote bootstrap and teardown scripts

**Files:**
- Create: `scripts/bootstrap-remote.sh`
- Create: `scripts/teardown-remote.sh`

- [ ] **Step 1: `scripts/bootstrap-remote.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail

CLUSTER_NAME="cloud-lab"
REGION="${DO_REGION:-fra1}"

command -v doctl >/dev/null || { echo "doctl not installed"; exit 1; }
test -f .env.remote || { echo "Create .env.remote from .env.remote.example"; exit 1; }

echo ">>> 1/8 Ensuring DOKS cluster exists"
if ! doctl kubernetes cluster get "$CLUSTER_NAME" >/dev/null 2>&1; then
  doctl kubernetes cluster create "$CLUSTER_NAME" \
    --region "$REGION" \
    --node-pool "name=default;size=s-2vcpu-4gb;count=2" \
    --wait
fi
doctl kubernetes cluster kubeconfig save "$CLUSTER_NAME"

echo ">>> 2/8 Installing ingress-nginx"
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx >/dev/null 2>&1 || true
helm repo update >/dev/null
helm upgrade --install ingress-nginx ingress-nginx/ingress-nginx \
  -n ingress-nginx --create-namespace \
  -f charts/ingress-nginx/values.yaml \
  --wait

echo ">>> 3/8 Waiting for LoadBalancer IP"
for i in {1..60}; do
  LB_IP=$(kubectl -n ingress-nginx get svc ingress-nginx-controller \
    -o jsonpath='{.status.loadBalancer.ingress[0].ip}' || true)
  [[ -n "$LB_IP" ]] && break
  sleep 5
done
[[ -n "$LB_IP" ]] || { echo "No LB IP after 5 min"; exit 1; }
echo "LB_IP=$LB_IP"
HOST="cloud-app.${LB_IP}.nip.io"

echo ">>> 4/8 Substituting ingress host"
sed -i.bak "s/CLOUD_APP_HOST/${HOST}/g" k8s/remote/ingress-patch.yaml

echo ">>> 5/8 Installing kube-prometheus-stack"
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts >/dev/null 2>&1 || true
helm repo update >/dev/null
helm upgrade --install monitoring prometheus-community/kube-prometheus-stack \
  -n monitoring --create-namespace \
  -f charts/kube-prometheus-stack/values.yaml \
  --set "grafana.ingress.hosts[0]=grafana.${LB_IP}.nip.io" \
  --wait

echo ">>> 6/8 Installing loki-stack"
helm repo add grafana https://grafana.github.io/helm-charts >/dev/null 2>&1 || true
helm repo update >/dev/null
helm upgrade --install loki grafana/loki-stack \
  -n monitoring \
  -f charts/loki-stack/values.yaml \
  --wait

echo ">>> 7/8 Applying k8s/remote overlay"
kubectl apply -k k8s/remote
kubectl rollout status deployment/cloud-app --timeout=300s

echo ">>> 8/8 Smoke test"
sleep 5
curl -sf "http://${HOST}/" && echo
curl -sf -X POST "http://${HOST}/messages" \
  -H 'Content-Type: application/json' \
  -d '{"text":"hello from cloud"}' && echo
curl -sf "http://${HOST}/messages" && echo

echo "App URL:     http://${HOST}/"
echo "Grafana URL: http://grafana.${LB_IP}.nip.io/  (admin/admin)"
echo "Restore ingress file: mv k8s/remote/ingress-patch.yaml.bak k8s/remote/ingress-patch.yaml"
```

- [ ] **Step 2: `scripts/teardown-remote.sh`**

```bash
#!/usr/bin/env bash
set -euo pipefail
CLUSTER_NAME="cloud-lab"
read -p "This will delete DOKS cluster '$CLUSTER_NAME' and all data. Continue? [y/N] " ans
[[ "$ans" == "y" || "$ans" == "Y" ]] || exit 0
doctl kubernetes cluster delete "$CLUSTER_NAME" --force
echo "Deleted."
```

- [ ] **Step 3: Make scripts executable**

Run: `chmod +x scripts/bootstrap-remote.sh scripts/teardown-remote.sh`

- [ ] **Step 4: Checkpoint.**

---

## Task 14: CI workflow

**Files:**
- Create: `.github/workflows/build-and-push.yml`

- [ ] **Step 1: Create the workflow**

```yaml
name: Build and Push
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: read
  packages: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Run tests
        run: ./gradlew test --no-daemon

      - name: Build JAR
        run: ./gradlew bootJar --no-daemon

      - name: Log in to GHCR
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/cloud-app:latest
            ghcr.io/${{ github.repository_owner }}/cloud-app:sha-${{ github.sha }}
```

- [ ] **Step 2: Note for the user**

After the first successful push, manually flip the GHCR package visibility to **public** so DOKS can pull without `imagePullSecret`:
GitHub → your profile → Packages → `cloud-app` → Package settings → Change visibility → Public.

- [ ] **Step 3: Checkpoint.**

---

## Task 15: Demo runbook

**Files:**
- Create: `docs/demo-runbook.md`

- [ ] **Step 1: Write the runbook**

```markdown
# Cloud Lab — Demo Runbook

One command per README requirement, executable against either the local (`kind`) or remote (`DOKS`) cluster.

Set `HOST` first:
- Local: `export HOST=localhost`
- Remote: `export HOST=cloud-app.<LB-IP>.nip.io`

## Requirement 1 — Docker image
```bash
docker images | grep cloud-app
docker run --rm cloud-app:local java -version
```

## Requirement 2 — Published to registry
```bash
curl -sI https://ghcr.io/v2/infigo-adrian/cloud-app/manifests/latest | head
# Or: open https://github.com/infigo-adrian?tab=packages
```

## Requirement 3 — Deployed to K8s
```bash
kubectl get deploy,svc,ingress,hpa,statefulset
```

## Requirement 4 — K8s on cloud
```bash
doctl kubernetes cluster get cloud-lab
kubectl get nodes -o wide
```

## Requirement 5 — Internet-accessible
```bash
curl -i "http://${HOST}/"
```

## Requirement 6 — Scale
```bash
kubectl scale deployment/cloud-app --replicas=4
kubectl get pods -l app.kubernetes.io/name=cloud-app -w   # Ctrl+C when 4 are Ready
kubectl scale deployment/cloud-app --replicas=2
```

## Requirement 7 — Zero-downtime updates
In one terminal:
```bash
while true; do curl -s -o /dev/null -w "%{http_code}\n" "http://${HOST}/"; sleep 0.2; done
```
In another:
```bash
kubectl set image deployment/cloud-app cloud-app=ghcr.io/infigo-adrian/cloud-app:sha-<NEW>
kubectl rollout status deployment/cloud-app
```
Confirm the curl loop stays at 200 throughout.

## Requirement 8 — Rollback
```bash
kubectl rollout history deployment/cloud-app
kubectl rollout undo deployment/cloud-app
kubectl rollout status deployment/cloud-app
```

## Requirement 9 — Monitoring
```bash
kubectl -n monitoring port-forward svc/monitoring-grafana 3000:80
# Open http://localhost:3000  (admin/admin), see "Cloud App" dashboard
```

## Requirement 10 — Autoscale
```bash
kubectl get hpa cloud-app
# Generate load:
kubectl run -it --rm load --image=busybox:1.36 --restart=Never -- \
  sh -c "while true; do wget -q -O- http://cloud-app:8080/messages; done"
# Watch:
kubectl get hpa cloud-app -w
```

## Requirement 11 — Centralized logging
In Grafana → Explore → Loki → query: `{app_kubernetes_io_name="cloud-app"}`
Or via CLI:
```bash
kubectl -n monitoring logs deploy/loki -c loki | tail
```

## Requirement 12 — Metrics export
```bash
kubectl port-forward svc/cloud-app 8080:8080
curl http://localhost:8080/actuator/prometheus | head
```

## Requirement 13 — Database in separate container
```bash
kubectl get pod -l app.kubernetes.io/name=postgres -o wide
kubectl exec -it postgres-0 -- psql -U cloudapp -d cloudapp -c "SELECT count(*) FROM messages;"
```

## Requirement 14 — Storage mounted to DB
```bash
kubectl get pvc
kubectl describe pvc data-postgres-0 | grep -E "StorageClass|Capacity|Used By"
# Persistence proof:
kubectl delete pod postgres-0
kubectl wait --for=condition=ready pod/postgres-0 --timeout=120s
kubectl exec postgres-0 -- psql -U cloudapp -d cloudapp -c "SELECT count(*) FROM messages;"
# Same count as before delete.
```

## Teardown (remote only)
```bash
bash scripts/teardown-remote.sh
```
```

- [ ] **Step 2: Checkpoint.**

---

## Task 16: End-to-end local smoke test

**Files:** none modified — this is the final verification.

- [ ] **Step 1: Run the local bootstrap**

Run: `bash scripts/bootstrap-local.sh`
Expected: all eight steps print, final curls return:
- `Hello World!`
- `{"id":1,"text":"hello from bootstrap","createdAt":...}`
- `[{"id":1,"text":"hello from bootstrap",...}]`

- [ ] **Step 2: Verify metrics endpoint is being scraped**

Run:
```bash
kubectl -n monitoring port-forward svc/monitoring-kube-prometheus-prometheus 9090:9090 &
sleep 3
curl -s 'http://localhost:9090/api/v1/targets' | grep cloud-app | head
kill %1
```
Expected: at least one target with `"health":"up"` and `job":"cloud-app"`.

- [ ] **Step 3: Verify a rolling update is zero-downtime**

In one terminal:
```bash
while true; do curl -s -o /dev/null -w "%{http_code}\n" http://localhost/; sleep 0.2; done
```
In another:
```bash
kubectl rollout restart deployment/cloud-app
kubectl rollout status deployment/cloud-app
```
Expected: the curl loop stays at `200` throughout the rollout.

- [ ] **Step 4: Verify rollback works**

```bash
kubectl rollout history deployment/cloud-app
kubectl rollout undo deployment/cloud-app
kubectl rollout status deployment/cloud-app
```
Expected: `deployment "cloud-app" successfully rolled out`.

- [ ] **Step 5: Verify Postgres persistence**

```bash
kubectl exec postgres-0 -- psql -U cloudapp -d cloudapp -c "SELECT count(*) FROM messages;"
kubectl delete pod postgres-0
kubectl wait --for=condition=ready pod/postgres-0 --timeout=120s
kubectl exec postgres-0 -- psql -U cloudapp -d cloudapp -c "SELECT count(*) FROM messages;"
```
Expected: same count both times.

- [ ] **Step 6: Checkpoint** — lab is functionally complete locally. Cloud bring-up (`scripts/bootstrap-remote.sh`) is the next manual step when the user wants the live demo.

---

## Spec coverage check

| Spec section | Tasks |
|---|---|
| §5 App changes | 1, 2, 3 |
| §6 Dockerfile | 4 |
| §7 K8s base | 5, 6, 7, 11 |
| §7.1 Local overlay | 8 |
| §7.2 Remote overlay | 9 |
| §8 Observability | 10, 11 |
| §9 CI | 14 |
| §10 Bootstrap scripts | 12, 13 |
| §11 Probes | 5 (deployment.yaml) |
| §12 Requirement traceability | 15 (demo runbook) |
| §13 Testing | 3 (IT), 16 (E2E smoke) |

All 14 README requirements traced via the demo runbook in Task 15. No placeholders. No undefined symbols (all types defined in Task 3 are used by Task 3's controller; manifest names are consistent across tasks).
